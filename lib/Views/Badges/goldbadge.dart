import 'package:flutter/material.dart';

class GoldBadge extends StatelessWidget {
  const GoldBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('GoldBadge'),
    );
  }
}
