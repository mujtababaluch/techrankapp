import 'package:flutter/material.dart';

class ExpertBadge extends StatelessWidget {
  const ExpertBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ExpertBadge'),
    );
  }
}
