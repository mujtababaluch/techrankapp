import 'package:flutter/material.dart';

class SilverBadge extends StatelessWidget {
  const SilverBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('SilverBadge'),
    );
  }
}
