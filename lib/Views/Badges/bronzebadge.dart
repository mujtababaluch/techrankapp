import 'package:flutter/material.dart';

class BronzeBadge extends StatelessWidget {
  const BronzeBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('BronzeBadge'),
    );
  }
}
